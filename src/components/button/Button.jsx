import Button from '@mui/material/Button';


/* export const ButtonMUI = (props) => {
    return <Button variant="outlined" type="submit">
        {props.label}
    </Button>
} */

export const ButtonMUI = (props) => {
    return <Button variant="outlined">
        {props.label}
    </Button>
}